<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.app.mapper.ProductQnaMapper">
	<insert id="insertQna" parameterType="com.sp.app.model.ProductQna">
		INSERT INTO productQna(qnaNum, productNum, memberId, secret, question, answer, 
			answerId, block, qnaDate)
		VALUES(productQna_seq.NEXTVAL, #{productNum}, #{memberId}, #{secret}, #{question}, #{answer, jdbcType="VARCHAR"}, 
			1, 0, SYSDATE)
	</insert>
	
	<update id="updateQna" parameterType="com.sp.app.model.ProductQna">
		UPDATE productQna SET answer = #{answer}, answer_id = #{answer_id}, answer_date = SYSDATE, 
			showQuestion = #{showQuestion}, block = #{block}
		WHERE num = #{num}
	</update>
	
	<delete id="deleteQna" parameterType="long">
		DELETE FROM productQna
		WHERE num = #{num}
	</delete>
</mapper>